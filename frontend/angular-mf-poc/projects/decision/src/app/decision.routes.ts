import { Routes } from '@angular/router';
import { DecisionComponent } from './component/decision/decision.component';

export const routes: Routes = [
  {
    path: '',
    component: DecisionComponent,
  },
];
